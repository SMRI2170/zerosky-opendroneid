# 🗺️ 超詳細開発ロードマップ：マイルストーン & 技術タスク

このドキュメントは、プロジェクトの各フェーズを「小さな成功（スモールウィン）」の積み重ねとして定義し、具体的な調査事項や技術スタックを整理したものです。

---

## 🟦 Phase 1: 技術検証 & プロファイリング
**ゴール**: 「本当にスマホで動くか？」の確証を得て、ハードウェアの制約を特定する。

- [ ] **1.1 rapidsnark デスクトップ環境の構築**
    - [ ] `iden3/rapidsnark` を GitHub からクローンし、macOS/Linux 上でネイティブビルド。
    - [ ] **調査**: `prover` 実行時の依存ライブラリ（`gmp`, `libsodium` 等）の特定。
- [ ] **1.2 既存回路でのベンチマーク計測**
    - [ ] 論文の `authV2.zkey` を使い、`rapidsnark` で証明生成を実行。
    - [ ] **技術タスク**: `/usr/bin/time -v` または `Valgrind Massif` を用いた最大メモリ消費量（Peak RSS）の記録。
    - [ ] **🌟 小さな成功**: PC上で証明生成が **5秒以内**、メモリ **2GB以内** で完了することを確認。
- [ ] **1.3 回路入力データの解析**
    - [ ] `input.json` の構造を解析し、Dart の `Map` クラスから生成可能な形式か確認。
    - [ ] **調査**: `Poseidon` ハッシュの入力（緯度・経度・時刻）のビット精度とスケーリング（定数倍処理）の仕様確認。

## 🛠️ Phase 2: Flutter ネイティブ基盤の構築
**ゴール**: Flutter から C++ 関数を呼び出し、ネイティブ側で証明生成エンジンを動作させる。

- [ ] **2.1 Android 向けクロスコンパイル設定**
    - [ ] Android NDK を用いた `rapidsnark` のビルドスクリプト (`CMakeLists.txt`) の作成。
    - [ ] **調査**: `napi` 依存を排除し、純粋な C API として export するための修正箇所の特定。
- [ ] **2.2 iOS 向けクロスコンパイル設定**
    - [ ] Xcode プロジェクトでの Static Library (`.a`) ビルド設定。
    - [ ] **調査**: iOS のビットコード (Bitcode) 有効化の要否、および CPU アーキテクチャ (`arm64`) 最適化フラグの選定。
- [ ] **2.3 Dart FFI (Foreign Function Interface) の実装**
    - [ ] `struct` データの受け渡しロジックの実装。
    - [ ] **🌟 小さな成功**: Flutter から C++ 側に数値を渡し、計算結果を Dart 側で受け取れる（FFI疎通確認）。
- [ ] **2.4 Witness 生成エンジンの選定と統合**
    - [ ] **調査**: `snarkjs` (WASM) と C++ ネイティブの `witnesscalc` の速度・メモリ比較。
    - [ ] **技術タスク**: `witnesscalc` を静的ライブラリとしてビルドし、`rapidsnark` と結合。

## 📱 Phase 3: アプリケーション機能 & データ連携
**ゴール**: IPFS から飛行ログを取得し、スマホ内蔵 GPS と照合して証明用データを準備する。

- [ ] **3.1 高速 Poseidon ハッシュの実装**
    - [ ] **技術タスク**: Dart 版 `poseidon` の速度検証。遅い場合は C++ (FFI) 側に移植。
    - [ ] **🌟 小さな成功**: スマホ上で 100 個の座標データのハッシュ化が **1秒以内** に完了する。
- [ ] **3.2 IPFS ゲートウェイ・クライアントの実装**
    - [ ] `http` パッケージを用いた CID 指定による JSON データ取得機能。
    - [ ] **調査**: Infura / Pinata の無料枠制限と、モバイルからの最適なフェッチサイズ。
- [ ] **3.3 マークルツリー・トラバーサル**
    - [ ] IPFS から得たノード群から、ルートハッシュを再計算するロジックの実装。
    - [ ] **🌟 小さな成功**: 取得した飛行ログが、オンチェーンのルートハッシュと一致することをスマホ内で検証できる。

## ⛓️ Phase 4: Web3 & ガスレス化
**ゴール**: ユーザーが暗号資産を持っていなくても、安全に証明をブロックチェーンに記録できる。

- [ ] **4.1 ユーザー鍵管理 (Wallet)**
    - [ ] `eth_sig_util` 等を用いた、デバイス内での秘密鍵生成とセキュアな保存。
- [ ] **4.2 Account Abstraction (ERC-4337) 統合**
    - [ ] **調査**: `Biconomy SDK` / `Pimlico` の Flutter 対応状況（Dart SDK 不在時の REST API 実装検討）。
    - [ ] **技術タスク**: UserOperation の作成と署名ロジックの実装。
- [ ] **4.3 Paymaster 連携**
    - [ ] **🌟 小さな成功**: スマホで生成した署名を Paymaster に送り、**ガス代 0** でテストネットに記録される。

## 🧪 Phase 5: 最適化 & 堅牢化
**ゴール**: 実用的な速度と安定性を確保し、ユーザー体験を完成させる。

- [ ] **5.1 メモリ・モニタリング & 回避**
    - [ ] **調査**: `Android Low Memory Killer (LMK)` の挙動調査。
    - [ ] **技術タスク**: 証明生成直前の `Flutter.gc()` 呼び出し、または重い UI アニメーションの動的停止。
- [ ] **5.2 バックグラウンド処理の検討**
    - [ ] **調査**: `WorkManager` (Android) / `BackgroundTasks` (iOS) を用いたバックグラウンド証明生成。
- [ ] **5.3 最終的な UX 調整**
    - [ ] **🌟 小さな成功**: 「ドローン検知 → 証明生成 → チェーン記録」までが、アプリを閉じていても全自動で完結する。

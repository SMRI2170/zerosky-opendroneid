# 🚀 スマートフォン(rapidsnark)を用いた完全分散型検証システムの提案

既存の「サーバー依存型」から、スマートフォン側でゼロ知識証明を生成する**「完全分散型（サーバーレス）アーキテクチャ」**への移行案をまとめました。

---

## 📍 1. アーキテクチャ変更の概要

| 項目 | 既存システム（論文構成） | 新システム（提案構成） |
| :--- | :--- | :--- |
| **スマホ** | 位置情報をハッシュ化して送信 | **IPFSからの取得・証明生成・送信すべて** |
| **サーバー** | IPFSデータ取得・Witness/Proof生成 | **完全廃止** |
| **ブロックチェーン** | サーバー経由で送信 | **スマホから直接送信** |
| **課題** | 中央集権的なボトルネック | **真のP2P/Web3アプリ** |

---

## 💡 2. 新しいデータフロー
1. **BLE検知とデータ取得**: ドローン（UAV）の信号を検知し、スマホ現在地のハッシュ群を計算。
2. **IPFSからの直接取得**: Infura等のゲートウェイ経由で、ドローンの最新のMerkle Treeデータを取得。
3. **スマホ内でのProof生成**: `rapidsnark`（C++ベースの高速ライブラリ）を用いて、手元のデバイス内でGroth16証明を生成。
4. **ブロックチェーンへの直接送信**: 生成されたProofをCallDataに変換し、Polygon Amoy等へ直接送信・検証。

---

## 🚀 3. 新構成のメリット
- **究極のプライバシー保護**: 位置情報のハッシュすら外部に送信せず、情報漏洩リスクを理論上ゼロへ。
- **単一障害点（SPOF）の排除**: サーバー停止に影響されない、自律的なシステムの構築。
- **運用コストの削減**: 高スペックサーバーの24時間稼働が不要。
- **処理の高速化**: `rapidsnark` のネイティブ最適化により、スマホ上でもサーバー級の速度を実現。

---

## ⚠️ 4. 実装に向けた技術的課題と解決策

### 課題1: Proving Key（zkey）のサイズ
- **問題**: 100MBを超える可能性があり、ダウンロードが負担。
- **対策**: 初回起動時にバックグラウンドでダウンロードし、ローカルにキャッシュする。

### 課題2: スマートフォンのメモリ(RAM)制限
- **問題**: メモリ使用量が2GB超え、アプリが強制終了(OOM)する危険。
- **対策**: `rapidsnark` の高いメモリ効率を活用し、FFI経由の緻密なメモリ管理を行う。

### 課題3: ガス代（トランザクション手数料）の負担
- **問題**: ユーザーがウォレットに暗号資産を持っていないと送信できない。
- **対策**: **Account Abstraction (ERC-4337)** を導入。Paymasterにより、運営側がガス代を肩代わりする。

### 課題4: Flutterとrapidsnarkの統合
- **問題**: C++コードをFlutterから呼び出す必要がある。
- **対策**: `dart:ffi` または iOS/Android のネイティブ経由でのラッパー実装。

---

## 🎯 5. 今後のステップ（開発ロードマップ案）
1. **PoC開発**: PC上で処理時間とピークメモリ使用量をプロファイリング。
2. **ネイティブ結合**: rapidsnark 静的ライブラリをビルドし、Flutterプラグイン化。
3. **アプリ実装**: IPFSクライアントと Account Abstraction SDK を組み込む。
4. **実機テスト**: Pixel 8等でのOOMクラッシュ防止と、証明生成速度の最終検証。


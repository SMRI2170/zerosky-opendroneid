# 更新ログ 2026-03-27 00:32:06 JST

## 対象範囲

このドキュメントは、直近の更新内容のみをまとめた記録です。

主に対応した内容は以下です。

- `rapidsnark` の Android ログが正常動作かどうかの確認
- 距離判定で現在使っているモデルの整理
- `rapidsnark_app` へスマホ実測値の取得機能を追加
  - GPS で緯度・経度
  - 気圧センサーで高度推定

---

## 1. rapidsnark の Android ログ確認

ユーザーから以下のような Flutter / Android ログが共有されました。

- `groth16_public_size_for_zkey_file native called`
- `groth16_public_size_for_zkey_file:0`
- `groth16ProverZkeyFile native called`
- `groth16Verifier native called`
- `userfaultfd: MOVE ioctl seems unsupported: Connection timed out`

### 判断

このログだけを見る限り、実装は動作していると判断しました。

理由:

- `groth16PublicBufferSize`
- `groth16Prove`
- `groth16Verify`

が順に呼ばれているためです。

### 補足

- `E/RapidsnarkExampleNative` は Android ログレベル上 `Error` に見えますが、必ずしも処理失敗を意味しません
- `userfaultfd` の警告も、今回の証明成功可否そのものとは別系統です
- 実際に以前の Pixel 8 実機確認では、証明成功画面まで確認済みでした

関連確認ファイル:

- [FlutterRapidsnarkPlugin.kt](/Users/smri/Downloads/cursor/m1/flutter-rapidsnark/android/src/main/kotlin/com/rapidsnark/flutter_rapidsnark/FlutterRapidsnarkPlugin.kt)

---

## 2. 距離判定方式の整理

ユーザーから「今は距離の判定は何を用いていますか？」という確認があり、現在の実装方式を説明しました。

### 現在使っている距離モデル

今の回路は以下の近似 3 次元距離を用いています。

- 緯度差
- 経度差
- 高度差

をそれぞれスケーリングして、

- `lat^2 + lon^2 + alt^2`

を比較しています。

### 緯度経度の扱い

- 緯度経度は `E7` 整数へ正規化
- 緯度係数: `11132000 cm / degree`
- 経度係数: `9128800 cm / degree`
- 高度は `cm`

### 特徴

これは

- ハバーサイン
- WGS84 の厳密測地距離

ではなく、

- 日本の都市部近距離を想定した平面近似
- そこに高度差を加えた 3D ユークリッド距離

です。

---

## 3. 新規要求: スマホで GPS と気圧センサーを使いたい

ユーザーから次の要求がありました。

- スマホ側で気圧センサーから高度を取得したい
- GPS から緯度・経度を取得したい

これに対応するため、`rapidsnark_app` に端末センサー入力機能を追加しました。

---

## 4. 既存コード調査

まず、ワークスペース内に既にある気圧センサーアプリを確認しました。

参照したファイル:

- [barometric_pressure_sensor_app/lib/main.dart](/Users/smri/Downloads/cursor/m1/barometric_pressure_sensor_app/lib/main.dart)
- [barometric_pressure_sensor_app/pubspec.yaml](/Users/smri/Downloads/cursor/m1/barometric_pressure_sensor_app/pubspec.yaml)

ここで確認した内容:

- `sensors_plus` に `barometerEventStream()` がある
- 気圧から高度を推定する式を既に使っている

加えて、`rapidsnark_app` 側の現状も確認しました。

確認ファイル:

- [rapidsnark_app/pubspec.yaml](/Users/smri/Downloads/cursor/m1/rapidsnark_app/pubspec.yaml)
- [AndroidManifest.xml](/Users/smri/Downloads/cursor/m1/rapidsnark_app/android/app/src/main/AndroidManifest.xml)
- [Info.plist](/Users/smri/Downloads/cursor/m1/rapidsnark_app/ios/Runner/Info.plist)

---

## 5. 依存関係の追加

`rapidsnark_app/pubspec.yaml` に以下を追加しました。

- `geolocator: ^14.0.2`
- `sensors_plus: ^7.0.0`

変更ファイル:

- [pubspec.yaml](/Users/smri/Downloads/cursor/m1/rapidsnark_app/pubspec.yaml)

---

## 6. 権限設定の追加

### Android

`AndroidManifest.xml` に以下を追加しました。

- `ACCESS_COARSE_LOCATION`
- `ACCESS_FINE_LOCATION`

変更ファイル:

- [AndroidManifest.xml](/Users/smri/Downloads/cursor/m1/rapidsnark_app/android/app/src/main/AndroidManifest.xml)

### iOS

`Info.plist` に以下を追加しました。

- `NSLocationWhenInUseUsageDescription`

変更ファイル:

- [Info.plist](/Users/smri/Downloads/cursor/m1/rapidsnark_app/ios/Runner/Info.plist)

---

## 7. アプリ本体への機能追加

### 追加したこと

`rapidsnark_app/lib/main.dart` に以下を実装しました。

#### 1. 気圧センサーストリームの監視

- `barometerEventStream()` を購読
- 現在の気圧を `_currentPressureHpa` に保持
- 推定高度を `_currentAltitudeMeters` に保持
- 最終読込時刻を `_lastSensorReadAt` に保持

#### 2. GPS + 気圧センサーで第三者側ログを埋める関数

追加メソッド:

- `_fillTargetFromDeviceSensors()`

この中で実施していること:

- 位置情報サービス有効か確認
- 権限状態確認
- 必要なら位置情報権限をリクエスト
- GPS から現在地取得
- 気圧センサーから高度推定
- 第三者側入力欄へ自動反映
  - 緯度
  - 経度
  - 高度
  - 時刻

#### 3. センサーカード UI

追加 UI:

- `端末センサー取得`
- 気圧
- 推定高度
- 最終更新時刻
- `第三者側検知ログをGPS/気圧センサーで入力` ボタン

#### 4. 役割

今回の実装では、スマホ実測値は

- `第三者側検知ログ`

を埋める用途です。

つまり、

- ドローン側ログ: 既存どおり外部ログや想定値
- 第三者側ログ: 端末実測値

という役割分担です。

変更ファイル:

- [main.dart](/Users/smri/Downloads/cursor/m1/rapidsnark_app/lib/main.dart)

---

## 8. 高度推定の方法

気圧センサーからの高度推定は、既存の気圧センサーアプリと同様に以下の式を使っています。

```text
44330.0 * (1.0 - (pressure / 1013.25)^(1 / 5.255))
```

これは

- 標準気圧 1013.25 hPa を基準にした近似高度

です。

### 注意点

- GPS 高度ではありません
- 海面更正や現地気圧補正までは入っていません
- あくまで PoC / 現場推定値として扱う設計です

---

## 9. 実装中に発生したエラーと修正

### 1. 追加メソッドがクラス外に出てしまった

最初のパッチ後、以下のような Dart エラーが出ました。

- `Undefined name '_barometerSubscription'`
- `Undefined name 'mounted'`
- `The function 'setState' isn't defined`

原因:

- `_startBarometerStream()`
- `_fillTargetFromDeviceSensors()`

が `_SafetyUseCasePageState` クラス外に落ちていたためです。

対策:

- 両メソッドを State クラス内へ移動

### 2. nullable な `double?` の扱い

次に以下のエラーが出ました。

- `toStringAsFixed cannot be called on double?`

原因:

- `altitudeMeters` が nullable 扱いのまま `setState` 内で使われていたため

対策:

- 非 null が確定した値をローカル変数へ固定
- 不要な null-aware 式を整理

---

## 10. 依存反映と検証

### `flutter pub get`

新規依存を追加したため、パッケージ解決を行いました。

追加導入された主なパッケージ:

- `geolocator`
- `geolocator_android`
- `geolocator_apple`
- `sensors_plus`

### `flutter analyze`

最終結果:

- `No issues found!`

### `flutter test`

最終結果:

- `All tests passed!`

---

## 11. 現在の状態

今回の更新により、`rapidsnark_app` は以下の構成になりました。

- ドローン側ログは既存どおりシナリオ入力
- 第三者側検知ログはスマホ実測で自動入力可能
  - GPS: 緯度・経度
  - 気圧センサー: 高度推定
- そのまま witness 計算と Groth16 証明へ接続可能

つまり、PoC としては

- 実機センサー取得
- ZK 証明入力への接続

までが一段進んだ状態です。

---

## 12. まだやっていないこと

今回の更新では、以下はまだ未対応です。

- Pixel 8 実機で新しい `GPS/気圧センサー入力` ボタンを押して最終確認
- センサー未搭載端末向けの graceful fallback
- 標準気圧補正や既知標高校正
- ドローン側ログも BLE / 外部データから自動入力

---

## 13. 次の推奨ステップ

次にやるなら、以下が自然です。

1. Pixel 8 で `第三者側検知ログをGPS/気圧センサーで入力` を押す
2. 位置情報権限ダイアログを確認
3. 実測値が第三者側ログ欄に入ることを確認
4. そのまま `住民端末で Generate And Verify Proof` を押す
5. 実測値からの証明成功まで確認


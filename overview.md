# 🌐 プロジェクト全体像：プライバシー保護型・UAV安全検証システム

このドキュメントでは、本システムの社会的意義、ステークホルダー（利害関係者）、および想定される具体的なユースケースを定義します。

---

## 👁️ 1. ビジョン
**「法遵守の客観的証明」と「プライバシーの完全保護」を両立する、次世代のドローン運航基盤の構築。**

ドローンが社会実装される上で最大の壁である「墜落リスクへの不安」と「位置情報公開によるプライバシー懸念」を、ゼロ知識証明（ZK-SNARKs）とブロックチェーンで解決します。

---

## 🤝 2. ステークホルダーと役割

| ステークホルダー | 役割・メリット |
| :--- | :--- |
| **ドローン運航者 (Operator)** | 飛行ルートや時刻などの営業秘密を隠したまま、安全距離（30m）の遵守を第三者に証明できる。 |
| **一般市民・歩行者 (Bystander)** | 自分の位置情報を誰にも知られずに、ドローンが自分から安全な距離を保っていたかを確認できる。 |
| **規制当局 (Regulator/航空局)** | 膨大な飛行ログを人間がチェックすることなく、ブロックチェーン上で自動的かつ確実に法遵守を監査できる。 |
| **システム提供者 (Platform)** | サーバーレス構成により、低コストで透明性の高い検証プラットフォームを運営できる。 |

---

## 🎬 3. 具体的なユースケース（利用シナリオ）

### 📦 ユースケース A：都市部でのラストワンマイル配送
- **状況**: 物流ドローンが住宅街の上空を通過して荷物を届ける。
- **課題**: 配送ルートは企業の競争力（機密）であり、住民の位置情報も守る必要がある。
- **解決**: 住民のスマホがドローンのBLE信号を検知。住民は自分の位置とドローンの位置のハッシュのみを用いて「30m離れていた」ことを証明し、チェーンに記録。
- **結果**: 企業はルートを隠し、住民は位置を隠しつつ、安全な運航が行われたという社会的合意が形成される。

### 🎥 ユースケース B：イベント会場での空撮・警備
- **状況**: お祭りやスポーツ大会などの混雑した会場でドローンが撮影を行う。
- **課題**: 観客のプライバシー（誰がどこにいたか）を保護しつつ、航空法の「第三者との距離」を遵守する必要がある。
- **解決**: 観客のスマホアプリがバックグラウンドで安全性を自動検証。
- **結果**: 万が一、距離が近づきすぎた場合は即座にアラートを出し、事故を未然に防ぐとともに、事後に「全時間帯で安全が保たれていた」ことを証明できる。

### ⚖️ ユースケース C：苦情・紛争の解決（Dispute Resolution）
- **状況**: 「ドローンが自分の家のすぐそばを飛んでいた」と住民から苦情が入る。
- **課題**: 運航者は「正しく飛んでいた」と主張するが、互いのデータを開示するのはリスクが高い。
- **解決**: 住民が持つ「検知ログ」と運航者の「飛行ログ」を、第三者の検証回路（Circom）で突合。
- **結果**: ゼロ知識証明により、「実際に30m以内に入っていたか否か」の真偽値だけが導き出され、公平な判断が下される。

---

## 🛠️ 4. 検証の全体フロー（エコシステム）

1. **検知**: 物理世界（BLE）でのドローンとの接触。
2. **秘匿化**: 座標データのハッシュ化（Poseidon）。
3. **取得**: IPFSからドローンの飛行ログ（Merkle Tree）を取得。
4. **証明**: `rapidsnark` を用いたデバイス内での数学的証明生成。
5. **検証**: ブロックチェーン上のスマートコントラクトによる完全な検証。
6. **記録**: 改ざん不可能な「安全の証跡」の保存。

---

## 🧪 5. このリポジトリでの実装方針

このワークスペースでは、次の実装を追加しています。

- `circuits/distance_30m_time.circom`
  - 緯度・経度・高度・時刻から `30m以内` かつ `5秒以内` を判定する Circom 回路
- `scripts/build_distance_assets.sh`
  - `circom` と `snarkjs` を使って Groth16 用アセットを生成するスクリプト
- `rapidsnark_app`
  - `circom_witnesscalc` で witness 計算
  - `flutter_rapidsnark` で Groth16 証明生成
  - verification key による検証
- `zk-distance/README.md`
  - 必要環境とセットアップ手順

### 判定ロジック

- 入力値
  - ドローン: 緯度・経度・高度・時刻
  - 対象: 緯度・経度・高度・時刻
- 条件
  - 空間距離が 30m 以下
  - 時刻差が 5秒 以下
- 緯度経度は回路内で扱いやすいよう `E7` 整数に変換
- 高度は `cm`
- 時刻は `Unix time sec`

詳細は `zk-distance/README.md` を参照してください。

---

### 💡 結論：本システムがもたらす未来
このシステムは、単なるドローンの監視ツールではなく、**「データの中身を見せずに、その正しさだけを信じる」**という新しい信頼の形を社会に提供します。これにより、ドローン物流や自動運転、さらにはスマートシティ全体のプライバシー保護インフラとしての応用が期待されます。

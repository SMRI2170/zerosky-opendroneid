# Update Log 2026-03-27 01:03:53 JST

## 対象

この更新ログは、直近の作業として実施した以下の内容をまとめたものです。

- `rapidsnark_app` における BLE 受信対応
- `uav/remote_id.py` を使った UAV 側 BLE 配信対応
- スマホ側の GPS / 気圧センサー取得との統合
- Pixel 8 上での起動確認
- 関連ドキュメント更新

## 背景

もともとのアプリは、ドローン側ログと第三者側ログを手入力して、Circom 回路と `snarkjs` / `rapidsnark` により `30m 以内 + 5秒以内` をゼロ知識証明する構成だった。

その後、ユースケースに寄せるために以下を拡張した。

- 第三者側ログはスマホの実測値を使えるようにする
- ドローン側ログは UAV からスマホへ直接配信する
- スマホは受信した UAV ログと自端末ログを組み合わせて、その場で証明生成と検証を行う

## 今回やったこと

### 1. 距離判定の現状を整理

現在の回路は、厳密な測地距離ではなく、都市部の近距離ユースケース向けの平面近似を用いている。

- 緯度経度は `E7` 整数へ正規化
- 高度は `cm`
- 時刻は `Unix sec`
- 緯度差に `11132000 cm / degree`
- 経度差に `9128800 cm / degree`
- 高度差をそのまま使用
- `lat^2 + lon^2 + alt^2` による 3 次元距離で `30m` 以下を判定
- 時刻差は `5秒` 以下を判定

### 2. スマホ側で GPS / 気圧センサーを使うようにした

`rapidsnark_app` に以下を追加した。

- `geolocator`
  - スマホの現在緯度・経度を取得
- `sensors_plus`
  - 気圧センサー値から高度を推定

気圧からの高度推定には以下を使用している。

```text
44330 * (1 - (P / 1013.25)^(1 / 5.255))
```

この値で `第三者側検知ログ` を自動入力できるようにした。

### 3. rapidsnark の Android ログを確認し、実装が動いていることを整理

Android の `logcat` に出ていた以下のようなログについて、実際には失敗ログではなく、`groth16` のネイティブ処理が呼ばれている痕跡であることを確認した。

- `groth16_public_size_for_zkey_file native called`
- `groth16ProverZkeyFile native called`
- `groth16Verifier native called`

あわせて、画面上の成功表示や前回の Pixel 8 実機確認結果と矛盾しないことも整理した。

### 4. UAV 側 Raspberry Pi プログラムを BLE テレメトリ配信型へ作り替えた

`uav/remote_id.py` は最初、固定値の iBeacon / Eddystone / カスタムデータを流すだけのサンプルに近い状態だった。

これを、UAV の位置情報・高度・時刻を BLE 広告としてスマホへ配信する実装へ差し替えた。

新しい `uav/remote_id.py` の内容:

- `TelemetrySample` を定義
- `StaticTelemetryProvider`
  - `--lat --lon --alt` で与えた固定値を配信
- `JsonTelemetryProvider`
  - JSON ファイルから `latitude / longitude / altitude_m / unix_time_sec` を読んで配信
- manufacturer data を構築
- `hcitool` / `hciconfig` で BLE 広告を有効化
- 1 秒ごとに広告データを更新

### 5. UAV の BLE パケット形式を定義した

スマホ側と UAV 側で一致するように、manufacturer data の中身を次の形式に統一した。

- `company_id = 0x02E5`
- `magic = 0x5A`
- `version = 0x01`
- `lat_e7` signed int32 little-endian
- `lon_e7` signed int32 little-endian
- `altitude_dm` signed int16 little-endian
- `unix_time_sec` uint32 little-endian
- `sequence` uint8

これにより、スマホ側は BLE 広告からそのまま UAV の `lat / lon / alt / time` を復元できるようにした。

### 6. スマホ側に BLE スキャン受信を実装した

`rapidsnark_app` に `flutter_blue_plus` を追加し、UAV の広告を検出できるようにした。

実装した内容:

- `UavTelemetryPacket` を追加
- manufacturer data を復号
- `company_id=0x02E5` の広告だけを対象化
- 受信成功時に以下を自動入力
  - `ドローン側緯度`
  - `ドローン側経度`
  - `ドローン側高度`
  - `ドローン側時刻`
- RSSI と sequence も UI 上で見えるようにした

### 7. アプリ UI に UAV BLE 受信カードを追加した

アプリに `UAV BLE 受信` カードを追加し、次の操作を可能にした。

- `UAV広告を受信して入力`
  - BLE をスキャンして、受信した UAV ログをドローン側ログへ反映
- `UAV受信後に証明実行`
  - UAV 受信後、そのまま Groth16 証明生成と検証を実行

これにより、ユースケース上のデータの流れは次のようになった。

1. UAV が BLE で自機位置・高度・時刻を配信
2. スマホがそれを受信してドローン側ログに反映
3. スマホ自身が GPS / 気圧センサーで第三者側ログを取得
4. スマホ内で witness 計算、Groth16 証明生成、検証を実施

### 8. 権限と依存関係を更新した

追加した依存:

- `flutter_blue_plus`

Android に追加した権限:

- `BLUETOOTH`
- `BLUETOOTH_ADMIN`
- `BLUETOOTH_SCAN`
- `BLUETOOTH_CONNECT`
- `ACCESS_COARSE_LOCATION`
- `ACCESS_FINE_LOCATION`

iOS に追加した説明:

- `NSBluetoothAlwaysUsageDescription`
- `NSBluetoothPeripheralUsageDescription`
- `NSLocationWhenInUseUsageDescription`

### 9. ドキュメントを更新した

`zk-distance/README.md` に以下を追記した。

- UAV 側の `remote_id.py` の使い方
- 固定値配信の例
- JSON ファイル配信の例
- manufacturer data のバイトレイアウト
- スマホ側での操作手順
- 必要権限

## 変更した主なファイル

- `uav/remote_id.py`
- `rapidsnark_app/lib/main.dart`
- `rapidsnark_app/pubspec.yaml`
- `rapidsnark_app/android/app/src/main/AndroidManifest.xml`
- `rapidsnark_app/ios/Runner/Info.plist`
- `zk-distance/README.md`

## 確認したこと

今回の更新に対して、以下を確認した。

- `python3 -m py_compile uav/remote_id.py`
  - 成功
- `flutter pub get`
  - 成功
- `flutter analyze`
  - 成功
- `flutter test`
  - 成功
- `flutter run -d 45061FDJH0007T`
  - Pixel 8 で起動成功

## 現時点でできていること

- スマホだけで第三者側ログを実測取得できる
- UAV 側 BLE 広告の送信コードがある
- スマホ側 BLE 受信コードがある
- 受信した UAV ログを証明入力へ直接つなげられる
- 端末内で Groth16 証明生成と検証を行う流れは維持されている

## まだ最終確認していないこと

この環境では、次の実機疎通はまだ未確認である。

- Raspberry Pi 上で `uav/remote_id.py` を実行
- Pixel 8 がその BLE 広告を検出
- 受信した実パケットで `Generate And Verify Proof` まで成功

つまり、送信コードと受信コード、スマホアプリの起動までは確認済みだが、実 Raspberry Pi と Pixel 8 の BLE 無線経路を使った最終 E2E は次の確認対象である。

## 次にやるとよいこと

1. Raspberry Pi で `python3 uav/remote_id.py --lat ... --lon ... --alt ...` を起動する
2. Pixel 8 のアプリで `UAV広告を受信して入力` を押す
3. `第三者側検知ログをGPS/気圧センサーで入力` を押す
4. `UAV受信後に証明実行` を押す
5. `Public Signals` と `Proof JSON` が表示されることを確認する

## 補足

UAV 側スクリプトは `hcitool` / `hciconfig` を使っているため、Raspberry Pi 側には BlueZ 系ツールと `sudo` 実行環境が必要である。
